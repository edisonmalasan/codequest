# Phase 1 evidence report

Updated 2026-09-15 for [validate-codequest-technical-risks](../../openspec/changes/validate-codequest-technical-risks/tasks.md). Phase 1 continues at **22/34 tasks complete, Redesign/no-go pending dedicated retest**. The owner approved the [persistent dedicated-bootstrap experiment](proposed-bootstrap-revision.md), implemented through `9e5fb36` with private cleanup, fresh HTTP Workers and pinned public-byte/CSP offline-cache repair. Its 24 unit tests pass; final committed-build browser retests are pending. The prior opaque full installed-Chrome **34 passes / 15 failures** remain historical evidence. S01-S06, new outcome review, production selection and Proceed remain unresolved. No Phase 2, Sync or Archive. Older checkpoints preserve their original measured results.

## Environment and reproduction

See the [Windows inventory](evidence/windows-inventory.json): Windows 10 Pro 10.0.19045, build 19045, Intel Core i5-6500 at 3.20 GHz, four logical processors, 7.9 GiB RAM. The initial inventory did not find installed consumer browser versions. The current dated inventory confirms Chrome 153.0.8010.37; installed Firefox remains unconfirmed. No physical typing, installation, cold-launch or assistive-technology test is inferred.

The [disposable harness](../../prototypes/technical-risk-validation/README.md) contains exact commands, a pinned pnpm lockfile and synthetic fixtures. Implementation baseline is `408eec5`; mock-version and fingerprint corrections are at commit 8bef387. Automated JSON records retain their actual HEAD and dirty paths; records made before the implementation commit describe an uncommitted prototype, not a clean reproducible historical checkout. Node was v24.21.0, pnpm was Corepack-pinned 10.30.3, Playwright 1.63.0 and automated Chromium 153.0.8010.12. Chromium runs are headless with GPU disabled and renderer-process limit two; traces are off. These settings do not establish normal physical-browser performance.

App `http://127.0.0.1:4310`, runner `http://127.0.0.2:4311`, and request sink `http://127.0.0.1:4312` are loopback-only. Different hosts isolate application origin/storage; no production services, credentials, database, workspace or infrastructure were created. Servers serve public assets and simulate receipts; they never execute learner JavaScript. Physical-device HTTPS remains unconfigured.

## Reviewed legacy investigation verdicts (AP02)

These are historical executable-HTML candidate results. See the Worker-preview continuation below for current measured results; original failures remain evidence.

| Experiment | Available evidence | Verdict / missing evidence |
| --- | --- | --- |
| E01 editor | Q01 read/run/fail/hint/correct/check/save/reload, editor undo/indentation, simulated narrow viewport, explicit reset and source retention when changing tasks | Automated focused cases passed; physical input, paste/selection/touch/keyboard/rotation, frozen input-latency sampling, NVDA/VoiceOver and manual accessibility remain untested |
| E02 Worker lifecycle | Success, syntax/runtime errors, wrong-but-running check, loop and hostile Proxy termination, output flood/large/cyclic values, getter avoidance, fresh restart, stale cancellation, 100 opaque Worker cycles | Chromium and Firefox focused cases passed. Required physical desktop/mobile timing, retained-memory measurements and mobile cycles remain untested. Structured-clone allocation and browser memory have no proven hard quota |
| E03 execution boundary | Positive trusted/permissive controls reached the sink. Both restricted candidates denied fetch/XHR/WebSocket/import/importScripts/nested-worker/protected requests online; no application IndexedDB/cache canary was observed. Malformed and wrong-source messages rejected; output treated as text | Focused Chromium and Firefox cases passed; WebKit and physical coverage incomplete. Additional Chromium cross-origin BroadcastChannel/cookie-canary and oversized raw-message probes passed; hostile global-mutation/allocation coverage and hard quotas remain incomplete |
| E04 preview | Resource/form/top-navigation/popup/parent probes denied. Baseline self-navigation reached the sink. Parent-policy comparison stopped that request. Tight loop required emergency process termination | **Failed containment/recovery.** No production preview recommendation. Finite 1000-message flooding and ten explicit preview resets passed in Chromium; tight-loop recovery and other engine/device gates remain failed or untested |
| E05 persistence/PWA | Successful save and injected quota/unavailable failures, 20 reload cycles, public-only precache, prepared offline new page/local JS, waiting service-worker update, failed-save activation block, two-client draft retention | Automated cases passed after retained retest history. Same-process offline page is not a physical cold launch/install. Task-database clearing, failed-save reload recovery and the eight reload/six update/six multiclient mix passed in Chromium and Firefox. Physical storage exhaustion/background/cold recovery and other required modes remain untested; the bounded CDP quota diagnostic did not establish native quota enforcement |
| E06 mock integration | Q01 provisional flow, immutable pending source, guest/account isolation, explicit import, stable-quest dedupe, expiry/version rejection, forged local pass and lost-response retry | Focused browser/unit simulation passed; no authoritative production acceptance or independently verified learning claim. Integrated physical/mobile/preview/offline-update containment remains unverified |

## Candidate comparison and failure evidence

The opaque candidate uses a sandboxed `srcdoc` frame with `allow-scripts`, a Blob Worker and inherited CSP. Its policy is `default-src 'none'; script-src 'unsafe-inline' 'unsafe-eval' blob:; worker-src blob:; connect-src 'none'; frame-src 'none'; form-action 'none'; base-uri 'none'`. Public Worker bytes are inline bootstrap data; learner source is passed only after bootstrap. Sender checks bind the actual frame window; decoded results bind run/task/content/assessment identity.

The dedicated candidate uses a distinct-origin bootstrap with `allow-scripts allow-same-origin`; it is never an authenticated application origin. Bootstrap CSP allows only its own scripts/Worker, with connections/frames/forms denied. Worker response CSP is `default-src 'none'; script-src 'unsafe-eval'; connect-src 'none'; worker-src 'none'`. It depends on separately reachable bootstrap assets; physical HTTPS and offline dedicated-origin loading are unverified. No candidate is selected as production-safe. Opaque execution is the more promising Chromium/offline investigation candidate, subject to missing coverage and host-allocation limitations.

The baseline preview sandbox is `allow-scripts` without same-origin privilege. Its own CSP denies network resources, frames, connections and forms while allowing inline scripts/styles. Nevertheless, learner `location.href` produced **GET `/self-navigation?fixture=SELF_NAV`** at the sink. See [original evidence](evidence/automated-2026-09-13T06-21-23.357Z.json). A successful diagnostic test means evidence was collected; `policyPassed: false` is a security failure.

Adding parent `frame-src 'none'` at `/?framePolicy=none` denied the tested self-navigation and left opaque Worker execution usable in [serial evidence](evidence/serial-2026-09-13T06-28-05.003Z.json). It is only a comparison variant. It does not establish the dedicated frame's compatibility or comprehensive network denial across engines.

Even with that parent policy, `<script>while(true){}</script>` starved recovery. The [separate-process watchdog evidence](evidence/preview-loop-2026-09-13T06-31-47.393Z.json) records `PROBE_STARTED`, expiration of the 8-second emergency cutoff and termination of only the owned child/browser/server tree. B02 requires the 2-second execution deadline plus at most 1-second recovery. Emergency process termination fails that criterion. Origin restrictions do not establish CPU isolation.

## Verification and retained history

The timestamped [evidence directory](evidence/) retains initial failures and retests. The earlier complete Chromium run passed **22/22** cases including lost-response/version reconciliation and additional channel/message/preview-reset probes; earlier passing and failing runs are retained. See [earlier Chromium evidence](evidence/automated-2026-09-13T06-44-45.842Z.json), recorded against commit 140b114 with complete asset hashes frozen before trials. The 18 protocol/mock unit tests passed. Strict application TypeScript and separate WebWorker TypeScript checks, ESLint and the Vite/Serwist build passed. Built main JS is approximately 832 kB / 273 kB gzip with eight precache entries, approximately 819 KiB; Vite emits its large-chunk warning. These sizes are observations, not passing physical startup budgets.

Initial tests exposed a debounce race losing the latest draft during owner changes and a malformed quoted test fixture. Saving before owner/task transitions fixed source loss; both regression checks passed. Initial update verification reloaded during service-worker activation; the retest waits for actual activation, then verifies both clients' retained source. An initial records probe expected numeric `5`, but the documented protocol transports textual `"5"`; its assertion was corrected, with no change to protocol semantics. The [corrected serial retest](evidence/serial-2026-09-13T06-45-02.049Z.json) passed the records fixture; the original failed assertion remains retained.

Some earlier browser/check attempts failed with Windows committed-memory exhaustion (`VirtualAlloc`, V8 allocation failure, worker exit 134/3221226505 or crashed targets). They do not establish a learner-boundary failure or a browser pass. Runs were retried serially with reduced process overhead; no unrelated user processes or system pagefile settings were changed. One initial full-suite attempt was stopped by terminating only its owned Playwright process tree and did not produce a completed reporter file. One watchdog startup attempt itself exhausted memory before producing a record; the later completed watchdog record above establishes the actual preview failure.

The initial Firefox dependency preflight reported missing `msvcp140_1.dll`, leaving that attempt untested; later actual Firefox launch and passing focused cases are recorded below. WebKit launched but could not create a page, so WebKit cases remain untested/inconclusive, not Safari passes. No runtime libraries were installed system-wide.

`pnpm test`, `pnpm lint` and `pnpm typecheck` were attempted at repository root: each failed because `pnpm` is not on PATH. `corepack pnpm@10.30.3 test` at root reported `ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND`: there is deliberately no root production package. Prototype checks ran inside their own directory; no fake production commands were added.

Process/evidence capture deviation: the charter required exact asset hashes before the first candidate measurement, but those first dirty-build runs did not freeze a complete manifest. The serial runner freezes the listed asset SHA-256 values before its trials. Those later hashes do not retrospectively certify earlier builds; the final 22-case Chromium run freezes the complete built asset manifest before trials. Required physical/other-engine trials still need the same prospective capture. B01/B07/B08/B09 physical criteria are unchanged.

## AP02 legacy review and remaining gate (historical)

Project owner is the assigned technical/product/security reviewer. Security review is a **project-owner self-review**, not an independent audit. The project owner approved the no-go conclusion on 2026-09-13; see [AP02](../decisions.md#ap02-reviewed-phase-1-no-go). F01 is reviewed no-go for the evaluated previews, pending scoped redesign; F02 is inconclusive. ADR 0004 retains its accepted policy and records the measured failed feasibility. P01–P15, U01/AP01 and F03–F09 deferrals are preserved.

Earlier next decision, subsequently resolved by the explicit redesign instruction: review the preview failures and authorize a scoped containment/recovery redesign or an explicit no-go conclusion. Do not weaken P11, remove required preview behavior, add remote runners, or treat the comparison CSP as an accepted solution without that decision. Required device/accessibility/consumer-browser information remains unavailable or unconfirmed as recorded in [coverage](coverage.md). Use the [manual evidence checklist](manual-checklist.md) when access is explicitly supplied.

The active Apply task list retains incomplete acceptance gates. PR #5 integrates the verified investigation under the current Git workflow. Only fully completed tasks are checked; no production architecture is selected.

Teardown checkpoint: no listeners remained on prototype ports 4310/4311/4312 after the final automated and serial runs. Task browser contexts/services were closed or terminated by their owning harness; no certificate, hosts, DNS or ordinary browser-profile changes were made. Dependency/build outputs remain ignored and are not production artifacts.

## Continued Apply findings — 2026-09-13

Task 7.1 is now complete: schema revision 2 stores versioned public lessons keyed by task/content/assessment identity with SHA-256 response hashes. Download identity mismatches are rejected, saved drafts remain unchanged, and missing offline lessons are disclosed independently of draft availability. The synthetic endpoint is confined to public prototype fixtures; no production curriculum API or compiler exists.

The frozen B09 mix of eight reload, six waiting/activation update and six multiclient cycles passed in automated Chromium, preserving exact confirmed source and immutable pending identity. Explicit clearing deletes only the task database after closing its connection; confirmed local source loss is disclosed, and an offline missing-lesson message is truthful. Failed unconfirmed saves reload the last confirmed source. These results do not establish physical background/relaunch, browser installation or required other-engine/device coverage.

A dedicated preview bootstrap at the existing runner origin now relays only bounded HTML from the actual application parent into an inner opaque learner iframe. The application comparison header allows only the trusted runner bootstrap/preview document paths. Runner preview policy permits its own bootstrap script plus inline scripts/styles, denies connections/workers/frames/forms, and the inner document adds its restrictive policy. Inline JavaScript and useful total output were verified separately before hostile resource/self-navigation probing, which produced no sink requests in Chromium. A first combined fixture assertion lost its execution marker during navigation; the retained retest separates useful-rendering evidence from the navigation probe rather than weakening denial.

CPU recovery still failed after learner execution was verified. The default-browser comparison removes the extra GPU/renderer-process-limit flags; it also required emergency termination. This is headless automated evidence, not a physical consumer-browser claim. Adding an isolated-origin iframe did not establish independent recovery in either tested mode; no candidate or F01 mechanism is approved.

The quota experiment is diagnostic only. The initial assumption that a one-byte CDP quota override alone would produce a native error was rejected by actual successful writes. A bounded 8-MiB logical-source-pressure trial reported an active override but did not observe a native QuotaExceededError. That does not satisfy real-full-storage coverage. Injected failure handling, actual task-data clearing and last-confirmed-source recovery remain separate passing focused checks. A long-source assertion was corrected to read canonical CodeMirror state rather than virtualized DOM text; it preserves the full editable-source requirement. Original failures are retained, and no physical drive is filled.

Earlier checkpoint (superseded by AP02): Review-dependent tasks 9.3–9.5 remain pending. Continuing Apply is authorization for these experiments, not owner approval of the measured outcome. Required physical/assistive coverage remains untested; continued automated Firefox findings are recorded below. PR #5 stays draft; Phase 2, Sync and Archive remain unstarted.
Final continuation verification: [28/28 Chromium cases](evidence/automated-2026-09-13T07-12-52.437Z.json) passed against source commit ba58c73 with completed-build asset hashes frozen before trials. The separate [default-flags isolated-preview loop](evidence/preview-loop-2026-09-13T07-13-58.048Z.json), recorded against 750a455 with browser version, dirty paths and prospective hashes, failed trusted recovery and required owned-process termination. Diagnostic successes do not satisfy failed or inconclusive policy gates. Application/WebWorker strict TypeScript, ESLint, 18 unit tests and the Vite/Serwist build passed again; root pnpm test/lint/typecheck were retried and remain unavailable because pnpm is not on PATH. Built main JS is now approximately 834 kB / 273 kB gzip; ten precache entries total approximately 821 KiB. No listeners remained on ports 4310/4311/4312 after final teardown.

## Firefox availability and persistence corrections

The original missing-library message was Playwright host dependency preflight output, not proof that Firefox could not execute. An explicitly recorded `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1` run launched downloaded Firefox **155.0**. This skips only the dependency preflight; it does not relax assertions, CSP, security policies or physical-device criteria. No system library or global configuration was installed. Actual WebKit page creation still failed under the same preflight option; see [retained WebKit evidence](evidence/automated-2026-09-13T07-22-51.481Z.json). This is not Safari device coverage.

Firefox exposed two persistence races: a passing local check retained a previous Saved label before its provisional metadata write; and the asynchronous startup draft lookup could replace early edits. New completion is now explicitly unsaved until persistence succeeds. Editing and source actions stay disabled until draft loading completes, with a controlled 500-ms lookup regression. Programmatic source restoration is annotated separately from user edits so it cannot subsequently replace a confirmed Saved label with Unsaved edits. Original failures and retests remain in the evidence directory.

One verification attempt exhausted Windows committed memory, and a concurrent Firefox run reported a page crash. The TypeScript check then passed when rerun separately. These failures are retained rather than converted into passing resource-isolation claims. Other user processes and system page-file settings were left intact.

The [full Firefox run](evidence/automated-2026-09-13T07-37-11.652Z.json) passed **28 cases**, with the Chromium-CDP quota diagnostic explicitly skipped; commit aaa4e16 and frozen build hashes identify the tested code. The subsequent [reset/save-failure regression](evidence/automated-2026-09-13T10-55-32.546Z.json) passed with the later reset correction. The full run includes request/canary/message controls, 100 Worker cycles, public offline preparation, downloaded identity rejection and the exact B09 update/reload/multiclient mix. These are focused automated results, not physical Firefox or universal security certification.

Both Firefox preview loop candidates failed B02 after learner execution was independently observed: [dedicated preview](evidence/preview-loop-2026-09-13T10-56-10.373Z.json) and [opaque preview](evidence/preview-loop-2026-09-13T10-56-32.596Z.json). Each records Firefox 155.0, the preflight option, source commit 7c08a6a, prospective build hashes and emergency termination of only the task-owned tree. No candidate meets the required recovery gate; F01 remains unselected. Production remains blocked pending owner review of redesign/no-go and outstanding required physical/assistive evidence.

Latest Chromium verification: [29/29 cases passed](evidence/automated-2026-09-13T10-58-20.432Z.json) against source commit 7c08a6a, including the delayed-load and reset/save-failure regressions. The completed asset manifest was frozen before trials. This does not convert the diagnostic baseline navigation test, preview CPU failures or unenforced native quota diagnostic into passing gates.

Final available-check checkpoint: build, strict application/WebWorker TypeScript, ESLint and all 18 unit tests passed on the corrected source. Root `pnpm test`, `pnpm lint` and `pnpm typecheck` were attempted again and each remained unavailable because pnpm is not on PATH; no root application manifest was introduced. Strict OpenSpec validation, whitespace checks and 39 relative documentation links passed. Final teardown found no listeners on ports 4310/4311/4312. Timestamped command-check evidence records these results; source, synthetic failures and retests remain retained for owner review.

## Approved review disposition - 2026-09-13

[AP02](../decisions.md#ap02-reviewed-phase-1-no-go) is the approval evidence for Edison acting as technical, product and security reviewer. The review outcome is **Redesign/no-go for the evaluated preview mechanisms**, with **inconclusive required physical/accessibility coverage**. This is a project-owner self-review, not an independent security audit. The decision register, ADR 0004, architecture/frontend/security notes and roadmap now agree.

Tasks 9.3-9.5 are complete for the approved no-go outcome; 21/34 tasks are fully complete. The remaining 13 tasks retain their acceptance criteria and are not waived or marked passed: 1.2 physical inventory; 3.2 physical editing; 3.3 accessibility; 4.3 required runtime/resource trials; 5.2 required-environment security coverage; 6.1-6.3 usable contained/recoverable preview; 7.2 physical cold/offline/install; 7.3 native storage/background recovery; 7.4 required-environment persistence cycles; 8.2 required physical integrated loop; 8.3 selected-candidate preview/offline/update integration. Automated subsets remain linked above.

Next work requires physical/browser/assistive access and a scoped preview redesign with explicit design/acceptance artifacts before implementation. General task-completion authorization is not an unspecified policy revision or permission to invent evidence. The approved investigation can be merged; the change remains active with these incomplete gates. Do not begin Phase 2, Sync or Archive.

## Worker-preview redesign continuation - 2026-09-13

The owner explicitly requested a new Apply-continuation branch from updated main and a preview redesign preserving P11 and the approved limits. `spike/worker-preview-recovery` was created from merged Apply PR #5 (`cc27317`), immediately pushed, and represented by draft [PR #6](https://github.com/edisonmalasan/codequest/pull/6). Implementation commits `934fe53` and `8b48c58` are pushed. This authorization is not approval of a new production mechanism or evidence of missing physical tests.

### Mechanism and unchanged contract

`PreviewRuntime.runSource` executes current learner functions/records only in the existing restricted opaque Worker. Trusted host code starts the two-second deadline before bootstrap, terminates computation and removes presentation independently, bounds and validates results, escapes HTML metacharacters, and supplies a fixed script-disabled opaque iframe. The identical bounded result appears as a trusted-UI text equivalent. Learner-returned HTML/CSS is inert data; no learner DOM/event-plumbing curriculum or general HTML/JS IDE is introduced. Legacy executable-HTML candidates remain diagnostic comparisons and retain their AP02 no-go history.

P11's zero learner network/application/session/storage authority and independent timeout are unchanged. B02 remains two seconds plus at most one second recovery, B03 fresh readiness at most one second, and source/output/message/sample/cycle criteria remain B01-B09. Source preservation is checked against canonical CodeMirror state. A trusted run-bound dispatch marker immediately before Worker execution distinguishes actual loop trials from setup failures; it is not independent grading or a privileged learner message.

### Core automated preview results

| Engine | Ten loop trials: maximum recovery | Maximum fresh preview readiness | Executed reset cycles | Verdict |
| --- | --- | --- | --- | --- |
| Chromium 153.0.8010.12 | 2019.2 ms | 186 ms | 100 | Passed core automated gates |
| Firefox 155.0 | 2127 ms | 258 ms | 100 | Passed core automated gates |
| WebKit 26.6 | 2028 ms | 285 ms | 100 | Passed core automated gates |

These values come from the [final 102-case cross-engine run](evidence/automated-2026-09-13T15-34-48.252Z.json). Earlier completed runs include [Chromium 33/33](evidence/automated-2026-09-13T15-14-44.830Z.json), [Firefox 32 passed / one CDP-only diagnostic skipped](evidence/automated-2026-09-13T15-17-55.911Z.json), and [WebKit 28 passed / four failed / one diagnostic skipped](evidence/automated-2026-09-13T15-21-16.301Z.json). Each engine passed useful result 5 with matching textual output, hostile markup remaining inert with no active resource/navigation elements or sink requests, ten observed loop timeouts with host interaction and exact source retained, 100 actually executed preview starts/resets with no surviving iframe, and explicit protocol-flood rejection. These are headless results, not physical typing, Safari, cold-launch or assistive evidence. No hard memory/allocation quota is proven.

Separate owned watchdog trials also passed without emergency browser/process termination: [default-flags Chromium](evidence/preview-loop-2026-09-13T15-22-41.917Z.json), [Firefox](evidence/preview-loop-2026-09-13T15-25-52.793Z.json), and [WebKit](evidence/preview-loop-2026-09-13T15-26-04.502Z.json). Host use and `preview-timeout` within 3000 ms are explicit checks. Source commits, dirty paths, commands, browser builds, preflight setting and complete prospective build hashes are retained. Earlier dirty-build trials and failed diagnostic-query offline navigation remain original evidence; canonical prepared `/` offline/update retesting passed in Firefox.

### WebKit offline and version-fixture findings

WebKit now actually launches; its earlier page-creation failure no longer justifies an untested engine row. Its original full run failed three simulated-offline navigation/reload cases and a version-injection fixture. The original version fixture used intercepted network fulfillment behind a service worker; the controlled local server now returns assessment version 2 with a direct-request positive control. The [WebKit retest](evidence/automated-2026-09-13T15-21-55.110Z.json) verifies identity rejection and exact saved-source retention without skipping or weakening the requirement. Chromium and Firefox also passed the corrected server fixture.

The [WebKit offline diagnostic](evidence/automated-2026-09-13T15-23-15.129Z.json) records an activated controlling service worker, ten public precache entries and cached index status 200, while `context.setOffline(true)` makes both canonical navigation and a root fetch fail (`Load failed`). With networking enabled and the synthetic origin returning 503, the controlled-origin-outage fetch is served with status 200. This suggests the offline-emulation path differs from the reachable-service-worker path; it does not establish the cause or physical Safari offline success. Diagnostic collection passing is not the offline gate passing. The three offline policy failures remain blocking and are not skipped, relabeled or replaced with the weaker origin-outage diagnostic.

### Manual participation, review and progression

Only the physical Windows development machine is confirmed. Installed Chrome/Firefox availability/builds must be recorded during testing; no Playwright version is substituted. The [step-by-step manual checklist](manual-checklist.md) covers each installed Windows browser separately: physical edits and 10-warmup/100-sample latency; keyboard escape, feedback, 200%/400% zoom, 320-CSS-pixel reflow and reduced motion; ten Worker/preview loops and fresh starts, 100 executed cycles and resource observations; actual process-cold prepared offline/browser/install modes; native storage/background/restart recovery; exact eight reload/six update/six multiclient cycles; and the integrated quest and post-update containment loop. Physical-browser instrumentation is separate from physical typing. External disconnection alone leaves loopback reachable, so source-origin outage must be recorded too; reachable server assets cannot count as offline success.

macOS Safari, Android Chrome, iPhone Safari/home-screen, NVDA and VoiceOver remain **untested**, not passed or unsupported. Lower-powered hardware is not designated without an explicit declaration. Native full-storage failure and physical cold-launch timings remain unmeasured; injected failures and a non-enforcing CDP override do not replace them.

Task 6.1 is now implemented and verified. Task 9.3 is reopened for explicit review of this new recommendation; AP02 remains historical approval of the legacy no-go. Overall **21/34 tasks are complete; 13 remain incomplete**. Tasks 9.4/9.5 retain historical approved updates while the current register/ADR/roadmap explicitly show the unreviewed continuation. F01 remains a candidate recommendation pending review and required integration/physical evidence, F02 inconclusive, and production scaffolding blocked. The project owner's security review remains a self-review, not an independent audit. P01-P15/U01/AP01 and F03-F09 are unchanged. No Phase 2, Sync or Archive has begun.

### Final verification and fixture retest

The [final full cross-engine run](evidence/automated-2026-09-13T15-34-48.252Z.json) recorded **95 passed, five failed, two CDP-only diagnostics skipped**: Chromium 34 passed; Firefox 32 passed/one failed/one skipped; WebKit 29 passed/four failed/one skipped. All three engines passed the new core preview trials, and WebKit passed the corrected incompatible-assessment fixture. Two failures were first-acceptance tests sharing the synthetic server ledger across browser projects; their duplicate outcome was correct for the polluted fixture. The server now resets only the synthetic ledger between independent test cases. Acceptance/deduplication behavior and assertions are unchanged.

The [nine-case cross-engine retest](evidence/automated-2026-09-13T15-35-39.039Z.json) passed the affected first-acceptance and lost-response cases in every engine, plus the prepared-cache/origin-outage diagnostics. Each diagnostic now positively verifies a direct origin request receives 503 while the controlled page root fetch is served from cache with 200. WebKit still records `policyPassed: false` under the offline flag. The remaining three WebKit simulated-offline failures remain unresolved; the full suite is not reported as green by adding retest counts.

All 18 unit tests, prototype ESLint and strict application/WebWorker type checks passed. Root `pnpm test`, `pnpm lint`, and `pnpm typecheck` were actually attempted and fail because pnpm is not on PATH; Corepack equivalents were also attempted at root and fail with `ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND` because no root application manifest exists. No production command scaffolding was added. The [retained command-check evidence](evidence/command-checks-2026-09-13T15-38-52.878Z.json) and build manifest record final checks and teardown. Draft PR #6 remains unmerged while required available offline checks fail and physical/review gates are incomplete.

## Offline emulation and cold-origin diagnostics - 2026-09-14

The [minimal fixed-response service-worker control](evidence/automated-2026-09-13T21-08-50.997Z.json) removes React, Serwist, IndexedDB and learner execution. Chromium and Firefox deliver both online and emulated-offline requests to that worker and navigate successfully. WebKit delivers the online request, but the offline fetch fails with `Load failed`, navigation reports an internal error, and the worker fetch count stays at one. The worker still answers diagnostic messages. This localizes the observed failure before its fetch handler, without proving an exact upstream cause or real Safari behavior. Collection passed in all three engines; WebKit's `offlinePolicyPassed` is explicitly false. Existing application offline-emulation failures remain retained and unresolved.

The separate cold-origin probe creates an isolated persistent task profile, prepares public assets and exact saved Q01 source, awaits browser closure, stops its owned fixture server, verifies ECONNREFUSED at all three endpoints, and launches a new headless browser process with the same profile. This tests origin-unreachable cache recovery, not OS network disconnection or the required physical B07 timing gate.

| Engine/configuration | Evidence | Functional result |
| --- | --- | --- |
| Chromium 153.0.8010.12, prototype profile | [Cold-origin trial](evidence/cold-origin-2026-09-13T21-11-24.407Z.json) | Exact source, provisional local check and Worker-backed preview/text passed; observed readiness 583 ms |
| Firefox 155.0, prototype profile | [Cold-origin trial](evidence/cold-origin-2026-09-13T21-11-49.132Z.json) | Exact source, provisional local check and Worker-backed preview/text passed; observed readiness 1913 ms |
| WebKit 26.6, prototype profile | [Initial preparation failure](evidence/cold-origin-2026-09-13T21-12-43.599Z.json), [registration snapshot](evidence/cold-origin-2026-09-13T21-13-56.326Z.json), [cache-write snapshot](evidence/cold-origin-2026-09-13T21-15-01.656Z.json) | Inconclusive for cold recovery: online preparation never activated its worker; a bounded synthetic CacheStorage write reports `Failed writing data to the file system`, while the editor reports its separate IndexedDB save succeeded |
| WebKit 26.6, shorter task-only `temp/phase1-profiles` location | [Cold-origin comparison](evidence/cold-origin-2026-09-13T21-16-05.735Z.json) | Preparation, exact source, provisional check and preview/text passed; observed readiness 1992 ms |

Only the profile location changed in the WebKit comparison. The result demonstrates a path-dependent Windows test-profile constraint; a specific MAX_PATH/native-library cause is not established. It does not erase the failed longer-profile setup or establish Safari/macOS/iPhone persistence. Every invocation is one functional trial; observed times are not physical measurements or B07 ten-trial evidence. Prospective build/script/server/source hashes, actual HEAD/dirty paths, browser versions/options, transport controls and removal of only the newly created task profile are retained.

Method references: [Playwright offline emulation API](https://playwright.dev/docs/api/class-browsercontext#browser-context-set-offline) and [persistent-context lifecycle](https://playwright.dev/docs/api/class-browsertype#browser-type-launch-persistent-context). The observed engine behavior comes from the raw local controls, not an inferred fix from old upstream issues. No dependencies, security flags, P11 requirement, acceptance budget or existing failing assertion were changed.

Required physical Windows Chrome/Firefox procedures remain in the [manual checklist](manual-checklist.md). macOS/Android/iPhone/NVDA/VoiceOver remain untested; full Phase 1 stays 21/34 complete, with F01 review/selection and F02 coverage incomplete. PR #6 stays draft; no Phase 2, Sync or Archive begins.

Final focused WebKit verification retained [three application offline-emulation failures and one successful diagnostic collection](evidence/automated-2026-09-13T21-19-57.079Z.json). The [longer-profile repeat](evidence/cold-origin-2026-09-13T21-21-27.227Z.json) uses the same script hash as the successful shorter-profile comparison and again records a CacheStorage filesystem-write error before cold relaunch. These results are not combined into a green full suite. All 18 unit tests, prototype lint, strict application/service-worker type checks and build passed again; root `pnpm test`, `pnpm lint`, `pnpm typecheck` were actually retried and remain unavailable because pnpm is not on PATH. The root manifest remains absent. Original budgets and failing assertions are unchanged.

## Installed Windows Chrome continuation - 2026-09-14

The owner confirms installed Chrome and directs Codex to perform all accessible browser/development-environment checks without requesting manual validation. This session exposes no dedicated Browser Use or OS computer tool; Playwright/CDP and a narrowly targeted Win32 helper provide the available interaction. Ordinary browser profiles/windows, credentials, network adapters and OS session are not modified. Each persistent trial uses a fresh task-only profile and uninstalls its app before profile deletion.

Native browser inventory is [Chrome 153.0.8010.37 on the confirmed Windows machine](evidence/windows-inventory-2026-09-13T22-15-11.801Z.json): Windows 10 Pro 10.0.19045, HP ProDesk 600 G2 SFF, i5-6500, 8,464,728,064 bytes RAM, Intel HD Graphics 530 driver 31.0.101.2111, reported 1920x1080 display. Installed Firefox was not found at standard inspected paths and remains unconfirmed. Physical typing, low-powered designation/thermal conditions and assistive technology are not inferred.

### Native interaction and retained regressions

The [initial 35-case run](evidence/automated-2026-09-13T22-01-15.809Z.json) used headed installed Chrome with the initial fixed viewport: 31 passed and four failed. The [eight-case retest](evidence/automated-2026-09-13T22-02-30.356Z.json) passed the four failed runtime/preview cases but found new keyboard/reflow failures. Subsequent [ten-case verification](evidence/automated-2026-09-13T22-04-06.137Z.json) and [keyboard/persistence retest](evidence/automated-2026-09-13T22-11-07.933Z.json) retain all methodology and actual failures. Immediate source comparisons during asynchronous reload were corrected to wait for actual loaded/Saved state; no persistence acceptance was reduced.

Two real E01 issues were fixed: completion handling consumed Escape, and a configuration select overflowed at 320 CSS pixels. Highest-priority CodeMirror DOM key handling now moves focus to Run; bounded select widths preserve narrow controls. The [latest complete 39-case native run](evidence/automated-2026-09-13T22-18-28.884Z.json), now with viewport emulation disabled, passed all four extended interaction cases: keyboard typing/selection/undo/redo/Tab/Escape/activation/distinct errors/source reload, desktop 320px reflow, reduced-motion media/essential text, and 10 warmup plus 100 frame-proxy input timings. It finished **31 passed, eight failed**, with runtime/bootstrap/fresh-preview/lifecycle failures retained. This full run is not green. Native keyboard evidence is automated event handling, not physical input or NVDA/VoiceOver speech.

The [Escape investigation](evidence/automated-2026-09-13T22-11-46.420Z.json) and [pre-final keyboard run](evidence/automated-2026-09-13T22-13-09.545Z.json) retain failure details. Browser Ctrl+zoom shortcuts did not change actual geometry, so those attempts never passed native zoom. The separate [native settings/PWA trial](evidence/native-chrome-2026-09-13T22-20-59.934Z.json) confirms actual 200%/400% zoom through Chrome appearance settings: DPR 2/4 and viewport widths 464/232, exact source and local provisional check preserved. Screenshot hashes are retained as diagnostic artifacts; CDP raster capture is not a screen-reader or physical visual-usability certification.

### Runtime and window-state diagnostics

The native settings/PWA trial also collected all **100 Worker and 100 preview/reset cycles**, including the first ten warmup cycles within each total. Six valid Worker runs timed out at roughly 2 seconds; fourteen previews failed to become ready within 1 second. Every row records exact source retention and DOM cleanup. These are failed native lifecycle/fresh-run results, not only missing hardware evidence. No learner process was emergency-killed to obtain recovery.

Win32 inspected only disposable-profile Chrome children of the invoking test process. Its window was visible and not minimized, but SetForegroundWindow returned false and foreground ownership was not observed. Chrome's minimize/restore commands did not produce a hidden document visibility state. Background/foreground/physical OS interaction therefore remains inconclusive rather than passed. [Microsoft documents foreground-activation restrictions](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setforegroundwindow); that reference does not prove the cause of these local browser failures.

The [explicit occlusion-control comparison](evidence/native-chrome-2026-09-13T22-24-02.800Z.json) changes only the diagnostic process flag `--disable-backgrounding-occluded-windows`, with its own frozen hashes and profile. It still records **two Worker timeouts and one failed fresh preview**. This is not normal-browser or production-configuration evidence and does not resolve the native gate. An exact scheduling, process-retention, GPU, or upstream cause is not established. Original deadlines and all failed rows remain unchanged.

### Offline, persistence and actual PWA access

The [ten native cold-process trials](evidence/cold-origin-2026-09-13T22-05-00.671Z.json) await each task browser closure, preserve its isolated profile, stop the owned fixture server and verify ECONNREFUSED at all three origins before relaunch. All ten preserve exact source, provisional local check and useful preview/text; observed readiness is 1124-1598 ms. This is actual installed-Chrome process-cold origin-unreachable evidence, not OS network disconnection, physical low-powered B07 or mobile/Safari evidence. The frozen build and actual options are recorded; it does not retrospectively certify later builds.

Native same-process offline/update, task-only clearing, interrupted/injected full/unavailable save recovery, owner/version/mock transitions and the exact B09 mix were exercised. The strengthened B09 case now changes source each cycle, edits while updates wait, preserves an immutable pending snapshot and uses a different owner/source in the second client. Its native retest passed. The bounded quota override still committed all 128 writes despite a reported one-byte quota; quotaFailureObserved is false. Native full-storage enforcement remains inconclusive, never passed because diagnostic collection succeeded.

The [first native PWA diagnostic](evidence/native-chrome-2026-09-13T22-06-56.293Z.json) retains an invalid relative manifest-ID instrumentation error. The [corrected trial](evidence/native-chrome-2026-09-13T22-08-37.339Z.json) installed/launched the task app but did not observe standalone display. Resolving the manifest ID against its origin and explicitly selecting standalone only in the task app's browser settings produced the later observed standalone mode. Both later native diagnostic packets confirm install/standalone launch, **8 reload / 6 editing-update / 6 multiclient persistence cycles**, immutable pending/source recovery and a browser-process-cold installed-app relaunch with all origins refusing connections. Each records successful task-app uninstall and profile removal. Mobile home-screen, background/OS restart, ten physical installed cold-launch timings and native full storage remain untested/inconclusive.

### Proposed scope and progression

The [proposed S01-S06 adjustments](proposed-scope-adjustment.md) identify exact requirements/tasks that currently mandate inaccessible hardware, spoken AT, physical timing, native quota/lifecycle and WebKit offline-emulation coverage. They defer support/release evidence and retain numbers/sample counts, P11, truthful source/save behavior and explicit technical/security/product self-review. **They are not approved or applied.** No manual validation is requested.

Inventory task 1.2 is complete; total Apply progress is **22/34**. Required native Chrome fresh-run/lifecycle failures remain in addition to the three retained WebKit offline-emulation failures. Missing equipment is still untested. The current recommendation is **Redesign/no-go for available native runtime/fresh-preview gates, with scope and result review pending**. Even approval of S01-S06 alone cannot justify Proceed while a remaining eligible gate fails. F01 is not selected for production; F02 must reflect exact tested configurations. AP01/AP02 scope is unchanged. PR #6 stays draft; no Phase 2, Sync or Archive.


### Source synchronization and native storage-denial correction

The [prior 117-case run](evidence/automated-2026-09-13T22-33-57.415Z.json) retained 109 passes, six failures and two CDP-quota skips: Firefox rapid keyboard source corruption, WebKit stale-source reloads in keyboard/B09 tests, and three WebKit offline-emulation failures. CodeMirror's old React-source mirror could replace newer editor text, and asynchronous status updates could leave an old Saved label visible for a new edit. Source replacement now occurs only during draft load/explicit reset; edits synchronously invalidate the save label, and completed saves only mark their exact current source Saved. A six-round zero-delay rapid-input regression verifies immediate source and saved reloads.

[Actual native blocked-storage startup](evidence/native-chrome-2026-09-14T02-20-30.170Z.json) independently denied local storage, session storage and IndexedDB but exposed an unguarded session-storage read that crashed the workspace. [The corrected native retest](evidence/native-chrome-2026-09-14T02-21-44.817Z.json) observes the same three denials, no page errors, editable exact source and a truthful Save failed/keep-copy warning. Denied service-worker registration is visibly reported. Only a new disposable Chrome profile's preferences changed; no ordinary browser policy or development disk was modified. This verifies unavailable storage, not native full-disk/quota enforcement.


The [subsequent 123-case run](evidence/automated-2026-09-14T02-31-08.710Z.json), frozen on bundle DewvbPrF, reports **109 passed / 10 failed / 4 skipped**. Chromium's 41 cases passed; Firefox's 39 executable cases passed with two unavailable CDP diagnostics skipped. WebKit rapid-input and extended keyboard regressions passed, but its second-client B09 draft and provisional reload still failed, three offline-emulation gates failed, and five cases reported browser/page closure or crash before their assertions completed. One separate PowerShell process also failed CLR initialization during this run; a specific memory/crash cause is not proven. These are retained failures, not retroactive passes.

The next correction immediately invalidates Saved while owner/task switching is pending, restores editable state with a failed-save warning if switching fails, and correlates save completion with the exact draft revision, including provisional completion. B09 records synthetic owner/selected-owner/source before and after each second-client reload. Separate online-after-update probes run independently of the failing WebKit offline navigation, with trusted sink and application-database controls. Strict type checks and build passed for bundle B1JBMD6x; subsequent tests identify their own frozen hashes.


The [21-case targeted retest](evidence/automated-2026-09-14T02-35-25.784Z.json) passed 20 cases, including all engines' post-update online authority probes and rapid-input/keyboard/provisional-reload regressions; WebKit B09 failed at page creation. Its [isolated B09 repeat](evidence/automated-2026-09-14T02-36-13.622Z.json) confirmed account-B's source had already become starter code before reload. Inspection found the intended pending-switch status edit had not landed in bundle B1JBMD6x. Bundle D69YLA7C contains that edit, but a later cycle still recovered the preceding account-B draft. The final correction also disables owner/task selection until the current draft finishes loading and during pending switches, preventing an initial loader from granting stale readiness during a switch. The delayed-load regression checks both selectors; B09 additionally checks exact source immediately after saving. Original failing packets remain retained.


The [pending-switch retest](evidence/automated-2026-09-14T02-39-46.463Z.json), bundle D69YLA7C, retained 11 passes and WebKit's older second-client source failure. The [final initial-load/B09 retest](evidence/automated-2026-09-14T02-42-51.823Z.json), bundle VZMd8kAk, passes all six cases across Chromium/Firefox/WebKit, including disabled initial selectors and exact 8 reload / 6 editing-update / 6 multiclient cycles. All exact typed/saved/reloaded second-client assertions remain intact. Source and regression fixes are committed/pushed at 278e110; 18 unit tests, lint and strict application/service-worker type checks passed after the final correction. No runtime deadline, P11 policy or production capability was changed.


## Final available-environment checkpoint - 2026-09-14

Current implementation source: 278e110; final app bundle VZMd8kAk. Per-record frozen build/script hashes identify every actual trial, including dirty diagnostic/planning artifacts. The [precise inventory](evidence/windows-inventory-2026-09-14T03-02-30.9426360Z.json) records Windows 10 Pro 22H2 **19045.6466**, HP ProDesk 600 G2 SFF, i5-6500, 8,464,728,064 bytes RAM, Intel HD Graphics 530 driver 31.0.101.2111 and Chrome 153.0.8010.37. Firefox was not found at standard paths or four specific App Paths registry keys; availability remains unconfirmed, not proven absent. No ordinary browser/profile/registry settings were altered by inventory.

| Evidence | Actual result | Progression meaning |
| --- | --- | --- |
| [Installed Chrome full suite](evidence/automated-2026-09-14T02-47-22.281Z.json) | **42 passed**. Rapid-input/source/undo/redo/focus/distinct-feedback, 320px desktop reflow, media preference, AX diagnostics, loop/restart/output/message/security, exact B09 mix, offline/update/mock and owned-renderer crash checks | Passing available cases, not a whole Phase 1 pass. Legacy comparison tests collect known policy failures; green collection does not approve executable HTML |
| [Native default lifecycle diagnostics](evidence/native-chrome-2026-09-14T02-49-20.566Z.json) | Ten actually observed Worker loops recovered at 2001-2013ms with fresh execution 38-181ms. Of 100 additional valid Worker runs, **two timed out** (76/77). Of 100 preview starts/resets, **one missed readiness** at 1000.3ms (95). Exact source and frame cleanup retained | Available native valid-run/repeated-start reliability failure; B03/B08 not waived |
| [Native natural-idle comparison](evidence/native-chrome-2026-09-14T02-53-49.439Z.json) | Ten verified Worker loops/fresh execution passed. 100 valid Worker runs succeeded, but preview 54 missed readiness at **1004.3ms**. Renderer counts 3 -> 50 -> 3 -> 3 before/after workload/ten seconds/thirty seconds natural idle. Documents settled to one and host heap about 6.2MB | No continuing retained-process/host-heap trend demonstrated in this comparison. No forced GC or new acceptance tolerance. One remaining fresh-preview failure still blocks Proceed |
| [Native ten-trial cold-origin recovery](evidence/cold-origin-2026-09-14T02-50-01.338Z.json) | **Ten passed**, native viewport, no offline emulation; each new Chrome process recovered exact source, versions/local provisional check and preview/text while all three origins refused connections. Readiness **1058-1361ms** | S04 evidence if explicitly approved; not physical OS network disconnection, low-power designation or physical B07 |
| [WebKit short-profile cold-origin recovery](evidence/cold-origin-2026-09-14T02-51-24.716Z.json) | Single actual headless trial passed at 996ms; exact source/check/preview, own profile removed | Supports disclosed test-configuration analysis, never a native Safari/physical/offline-emulation pass |
| [Nested Blob policy](evidence/automated-2026-09-14T02-55-08.988Z.json), [installed Chrome comparison](evidence/automated-2026-09-14T02-55-45.794Z.json) | Three bundled engines and installed Chrome passed. Permissive nested Blob control actually requested the sink; restricted candidates produced zero requests. Opaque nested Blob Workers are available, with inherited network denial. SharedWorker was undefined in the tested Worker globals | Do not claim every nested Worker constructor is blocked; HTTP and Blob paths differ. No new application/network authority observed |
| [Nested child-loop rejection/recovery](evidence/automated-2026-09-14T03-09-35.941Z.json), [installed Chrome](evidence/automated-2026-09-14T03-10-22.156Z.json) | Ten trials per restricted candidate passed in each tested engine. Fixed child-first-message fixture causes protocol rejection and runtime cleanup, with exact source and fresh execution <=1s; constructor/bootstrap denial recorded separately. Fixture does not explicitly terminate a running child | Additional available descendant-resource path, not a substitute for ten normative parent Worker/preview deadline trials. Unavailable child construction is not a child-loop execution pass |
| [Final native storage-policy denial](evidence/native-chrome-2026-09-14T02-56-07.059Z.json) | Local/session/IndexedDB denied; editor/source retained, failed-save and offline-preparation warnings, no page errors; only own task profile removed | Native unavailable storage passed. Quota/full-disk enforcement remains inconclusive; no physical disk exhaustion |
| [WebKit retained failures](evidence/automated-2026-09-14T02-56-45.796Z.json), [isolated startup diagnostic](evidence/automated-2026-09-14T03-00-11.310Z.json) | Seven-case retest: three passes/four failures, including **three unchanged offline-emulation failures** and a missing-UI reload failure. Isolated instrumented 20-reload retest then passed every exact-source cycle with 200 HTML response, correct bundle, no page errors/crashes | Original failed entry remains unexplained, not inferred source loss or waived by a later pass. S06 concerns offline emulation only; it does not waive other startup failures |
| [Installed Chrome instrumented 20 reloads](evidence/automated-2026-09-14T03-01-47.158Z.json) | Passed; exact source and public-only cache checks, bounded per-reload diagnostics | Confirms additional diagnostic capture without weakening the source assertion |

Native Chrome frame-proxy p95 was **14.7ms** after ten warmup plus 100 measured automated input events. This excludes physical key delivery and pixel presentation; B01 physical/lower-powered evidence is untested. Actual task-profile 200%/400% zoom independently changed DPR to 2/4, preserved source and provisional check and kept document width within viewport. CDP viewport-surface capture corrected the earlier clipped full-page screenshot artifact; inspected 400% capture shows readable Run/Check/Stop and vertical scrolling. Screen-reader comprehension, OS motion preference and physical visual certification remain untested.

Both latest native diagnostic packets installed the own-profile PWA, selected/observed standalone mode, ran exact 8 reload / 6 editing-update / 6 multiclient cycles, relaunched it in a new browser process with origins refused, checked exact source/provisional state, and successfully uninstalled only the task app before profile removal. This is CDP-controlled installed desktop-app evidence; mobile home-screen/OS restart/actual background lifecycle remains untested. Win32 restore was targeted only to task Chrome; foreground acquisition returned false. Minimize/restore did not establish hidden document state. Hard memory/quota bounds remain unverified. Resource snapshots are current owned-process counts/CPU sums and host metrics, not cumulative process CPU or total retained browser memory.

Available fixes and probes for the current approved candidate are recorded. **Phase 1 remains 22/34 tasks complete and Redesign/no-go for native startup/fresh-preview reliability, with inaccessible coverage and new recommendation review inconclusive.** Native full-suite passes do not erase independent failed rows. Exact sources, statuses and failed timings remain retained. S01-S06 are proposed, not approved/applied, and even approval of all six cannot make the remaining native gate pass. Technical/product/security review is pending from Edison as project owner; security is self-review, not an independent audit. AP01/AP02 remain unchanged. PR #6 stays draft. No Phase 2, Sync or Archive.

### Historical next-experiment recommendation (subsequently authorized)

The next proposed experiment is a bounded **persistent trusted opaque bootstrap with a fresh Worker per run**, rather than a new opaque document for every run. This is a hypothesis about avoiding repeated document/process startup, not a proven cause or approved production selection. It changes trusted bootstrap lifetime and control-channel ownership. The owner subsequently explicitly approved the D3/charter experiment revision and its implementation/retest; see the checkpoint below. This approval does not select a production mechanism or increase the candidate count.

The proposed security contract keeps the existing opaque origin/CSP and script-disabled fixed presentation, never evaluates learner source on a document thread, and keeps all B01-B09 numbers/sample counts. The bootstrap owns at most one current Worker, explicitly terminates it before acknowledging cleanup, revokes its public code Blob on disposal, and rejects stale run/task/version data. A private host/bootstrap MessagePort is never transferred to learner Workers; output remains a separate untrusted bounded channel. The host's independent deadline uses bounded acknowledgment followed by frame removal/invalidation if cleanup fails. No general learner DOM, auth/session/storage/network authority or remote runner is introduced. Bound trusted frame count per existing runtime instance; assert zero learner Workers after cleanup rather than hide retained bootstrap frames.

Validate prospective hashes, actual native foreground limitation, first/fresh startup, ten verified parent/preview loops, ten nested-child cases, 100 cycles, natural-idle resources, all online request/storage/channel/message vectors and exact offline/update/PWA/source behavior in every accessible engine. Keep <=1s fresh readiness and 2s+1s trusted recovery; any missed boundary remains no-go. Require explicit technical/security review of the lifecycle/control-channel revision before changing D3 or selecting it. Standards describe [Worker termination](https://html.spec.whatwg.org/multipage/workers.html#dom-worker-terminate) and [transferable message ports](https://html.spec.whatwg.org/multipage/web-messaging.html#message-ports); those API primitives do not establish this proposed candidate's security or performance.

The final nested-child regression observes the exact child-first-message marker from the actual current compartment window before counting a child-start rejection; generic bootstrap errors cannot substitute for that witness. Earlier status-only trials remain retained as weaker diagnostics. Both strengthened bundled/installed-Chrome retests passed; unavailable nested construction/bootstrap remains separately recorded, never a child-loop execution pass.


## Authorized persistent-bootstrap checkpoint - 2026-09-14

Approval evidence: the owner answered **"Yes i approve"** to the explicit request to revise D3/charter and implement/retest a persistent trusted opaque bootstrap with a fresh Worker per run while preserving P11 and every numerical limit. Contract commit `045eb0e`; initial experiment `fdb8ae3`; reproducible target diagnostic/fault-frame correction `3b36f44`; generation-bound output correction **`e9e8436`**. This is experiment authorization, not S01-S06 approval, F01 production selection, F02 support acceptance or a dated outcome review.

The private MessagePort is never transferred to learner Workers. Cleanup acknowledgments are correlated to run/task/content/assessment and a cleanup ticket; missing acknowledgments trigger the approved 100ms fallback within the original recovery budget. Acknowledgment records that trusted code invoked termination and cleared its current Worker handle, not synchronous death of every nested thread. One idle trusted bootstrap per runtime instance is counted separately from active learner Workers and presentation frames; explicit disposal closes ports, revokes public Worker blobs and removes bootstrap frames. Startup is not prewarmed outside the measured deadline.

An actual old output packet from the same persistent Window produced a protocol error in the next run before correction: [captured-output negative control](evidence/queued-output-replay-2026-09-14T07-47-20.260Z.json). The trusted bootstrap now captures immutable run identity and stamps a bounded learner-output envelope. The host checks the actual source Window, complete serialized 16KiB envelope and current identity, then independently validates the inner untrusted result. Superseded output is discarded. Public bootstrap IDs are correlation data, not secrets or authority. This fixes a confirmed replay defect; it does not establish the cause of unrelated timing failures.

| Recorded run | Actual results | Interpretation |
| --- | --- | --- |
| [Initial installed Chrome](evidence/automated-2026-09-14T07-39-17.365Z.json), initial `CpAIG2N9` bundle | 37 passed / 11 failed | Retained startup, recovery, fixture-frame lookup and other failures; not a passing baseline |
| [Initial bundled full suite](evidence/automated-2026-09-14T07-55-14.206Z.json), same frozen initial bundle | 132 passed / 6 failed / 6 skipped | Chromium target retention, Firefox 7021ms lifecycle failure, three WebKit offline failures and an overstrict retained-frame assertion; no retrospective pass |
| [Corrected affected-engine retest](evidence/automated-2026-09-14T08-07-06.927Z.json), `Cvq5Xrou` bundle | 111 passed / 5 failed / 4 skipped | Actual output replay, private control, bounded fallback, nested controls and preview cycles pass in all three engines. Chromium target retention, three WebKit offline cases and WebKit integrated Escape-focus assertion fail. Firefox prior deadline case and corrected fallback/frame-count assertion pass on retest; original failures remain |
| [Final installed-Chrome full suite](evidence/automated-2026-09-14T08-11-40.028Z.json), same corrected bundle | **45 passed / 4 failed** | Missing-ACK recovery fixture gets a valid-run timeout; useful preview has no result; verified-loop fresh preview misses 1s; hundred-preview cycle test exceeds 1s. Replay and native target test pass in this run, but these four failures independently prevent Proceed |
| [Bundled target diagnostic](evidence/worker-target-diagnostics-2026-09-14T08-13-10.546Z.json) | Two targets remain immediately and at +900ms; zero at +1000ms sample; Stop 82.9ms, fresh run 12ms | Separate reproducible observation does not erase the original failed <=1s target test. Target descriptors alone do not prove continuing execution or a hard resource bound |
| [Installed-Chrome target diagnostic](evidence/worker-target-diagnostics-2026-09-14T08-13-35.844Z.json) | Stop **1647.6ms**, fallback; fresh run 456.2ms; one target after explicit disposal sample | Stop exceeds the additional 1s recovery budget. Actual nested-body witness was not established here; descriptor/CPU measurements are diagnostic, not proof of execution or complete termination |
| [Corrected native cold-origin probe](evidence/cold-origin-2026-09-14T08-14-27.643Z.json) | Failed during provisional check: valid source timed out; **0/10 cold trials completed** | Earlier ten-trial pass on older code is historical only. Current S04 substitute is not satisfied; B07 physical testing remains untested. Owned profile removed |
| [Corrected native storage-denial probe](evidence/native-chrome-2026-09-14T08-14-58.722Z.json) | localStorage/sessionStorage/IndexedDB actually denied; editable source retained exactly and explicit save warning | Passed this bounded recovery check. Foreground helper could not establish foreground; native full disk/background/OS restart are not inferred |

The [final dated inventory](evidence/windows-inventory-2026-09-14-final.json) records Windows 10 Pro 22H2 **19045.6466**, HP ProDesk 600 G2 SFF, i5-6500, four logical processors, exact RAM/display values and installed Chrome **153.0.8010.37**. Firefox remains unconfirmed at inspected paths/registry keys. Actual native browser identity, frozen dist/script hashes, command lines, dirty documentation paths and failure errors remain in raw JSON. Bundled emulation is not physical mobile or Safari evidence.

Prototype **18 unit tests, lint, strict application/service-worker typechecks and production build passed** after `e9e8436` correction. Required root commands `pnpm test`, `pnpm lint`, `pnpm typecheck` cannot run: pnpm is not on PATH. The corresponding `corepack pnpm@10.30.3` root commands fail with `ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND`; root application scaffolding is outside Phase 1. No root package is introduced to manufacture passing checks.


### Final independent native and WebKit controls

[Corrected independent installed-Chrome diagnostic](evidence/native-chrome-2026-09-14T08-17-32.266Z.json), frozen `Cvq5Xrou` bundle at `e9e8436`: **10/10 actual parent-loop witnesses**, deadline/recovery 2001.3-2110.5ms and successful fresh runs 7.5-883.8ms. **96/100 valid Worker runs succeeded; cycles 80-83 timed out at 2004.7-2112.9ms.** The next valid run, cycle 84, succeeded in **1101.9ms**, exceeding the unchanged <=1s fresh-run budget. **100/100 preview cycles were ready**, maximum 179.7ms, exact editor source retained throughout. These passes do not erase the final native suite's failed preview cases or the independent Worker timeouts/fresh-run miss; reliability remains no-go.

Native actual 200%/400% zoom and essential source/check retention passed again. Installed PWA/standalone access passed; all **8 reload + 6 editing-update + 6 multiclient** cycles retained exact source and immutable pending snapshots. The installed app closed and relaunched with all three owned origins refusing connections, retaining exact source and provisional local check. This installed-app pass is distinct from the separate failed 0/10 Chrome cold-process probe and does not satisfy the required ten-trial S04 substitute. Task PWA uninstalled and task profile removed. Scoped Win32 foreground restoration succeeded in this run, but minimize/resume left document visibility `visible`, so real background lifecycle remains **inconclusive**. No OS restart, network-adapter disconnect or physical-input claim follows.

Renderer counts were 3 before, 7 after cycles, 4 after 10s natural idle and 4 after 30s; host heap approximately 6.21 to 6.43MB then stable, Documents 6 throughout. This is observational resource evidence, not a hard quota, immediate descendant-death proof or a relaxed recovery window. No forced GC or new memory threshold was applied.

[Isolated WebKit integrated focus retest](evidence/automated-2026-09-14T08-18-01.584Z.json) passed 1/1 with no code change; the preceding integrated Escape-focus failure remains retained and its cause unproven. [Corrected WebKit cold-origin control](evidence/cold-origin-2026-09-14T08-18-27.415Z.json) passed one headless prepared-process relaunch in 1109.9ms, exact source/provisional check/useful preview/text. Dependency preflight bypass is explicit in its command environment. This is not native Safari, physical B07 or a passing offline-emulation case. The three original emulation failures remain failed.

### Progression and remaining decisions

All technically available verification of this approved bootstrap candidate has been performed and retained. **22/34 tasks complete; 12 required gates remain incomplete. Phase 1 cannot truthfully be marked Proceed.** Available native valid-run, fresh-preview/fresh-run and Stop timing failures remain, plus the bundled target-lifetime failure and unproven descriptor/CPU interpretation. A passing repeat does not resolve intermittent failures. No numerical budget, P11 boundary, mandatory source-retention assertion or unsupported device result was waived.

[S01-S06](proposed-scope-adjustment.md) remain proposed, not approved or applied: defer inaccessible physical/browser/AT release evidence (S01/S02), explicitly permit the same-budget native input/frame and ten cold-process proxies (S03/S04), disclose unavailable native quota/background/restart proof (S05), and explicitly review the controlled WebKit offline-emulation limitation (S06). Even approving these cannot waive current native failures; the ten-trial Chrome substitute currently fails. Edison must subsequently record dated product, technical and **project-owner security self-review**, not an independent audit. F01 remains an unselected candidate; F02 is incomplete; AP01/AP02 and all approved Phase 0 policies remain unchanged.

Further redesign must first identify the native bootstrap-startup/termination cause and review any changed D3/security/lifecycle contract. Do not infer that a different origin, prewarming, unrestricted scripts, a longer budget or an unapproved remote runner solves it. No further architecture selection is made by this evidence report. PR #6 stays draft. Phase 2, Sync and Archive do not begin.


## Native startup investigation and terminal cleanup - 2026-09-14

The [instrumented native diagnostic without preceding hostile trials](evidence/native-startup-diagnostics-2026-09-14T08-55-38.643Z.json) completed 100 valid fresh runs within 1s before hostile trials. These runs use test-only document Worker/control timestamp instrumentation; they are not a replacement for uninstrumented acceptance. Source was the pre-correction `Cvq5Xrou` build.

[Opaque hostile-first stage diagnostic](evidence/native-startup-diagnostics-2026-09-14T08-56-25.686Z.json): 100 valid runs succeeded, but fresh runs 0/1 took **1031.5ms / 1904.9ms**, so the budget failed. Only **8/10 loop-start witnesses** were observed; trials 5/8 are not loop-execution passes. Run 0 reached replacement-bootstrap readiness around 650ms and output around 926ms, then used missing-ack fallback. Run 1 waited around **1863ms for bootstrap readiness**, constructed its Worker around 1883ms and received output around 1905ms. This locates substantial delay before Worker execution in these cases, without proving an OS/upstream cause or a cure.

[Existing dedicated-origin hostile-first comparison](evidence/native-startup-diagnostics-2026-09-14T08-57-34.732Z.json): **93/100 valid successes / 7 timeouts** (cycles 0,13-15,89,95-96); successful cycles 1,65,67 still exceeded 1s at 1434ms,1195.1ms,1885.1ms. Changing only origin while recreating the document per run does not pass. Diagnostic source was uncommitted when captured; each record freezes its actual script/build hashes and dirty paths. The committed diagnostic adds explicit marker error recording without retroactively filling earlier records.

[Read-only machine context](evidence/environment-context-2026-09-14-continuation.json) records raw WMI KiB values: FreePhysicalMemory 650012 of TotalVisibleMemorySize 8266336, FreeVirtualMemory 881408 of TotalVirtualMemorySize 15408780. This is machine-level context, not attributable learner memory or proof that memory caused the failures. No unrelated process, pagefile, browser profile or OS setting was modified. No remaining Chrome process using the repository task-profile path was found in the scoped inspection; this is not a scan or cleanup of ordinary Chrome profiles.

### Terminal-output correction and measured limits

Implementation **`d4d37f8`** invokes termination of the opaque candidate's Worker before forwarding any non-start-marker message, removing the host-control round trip before requesting termination. The exact correlated execution-start marker stays nonterminal. Independent host decoding, generation bounds, private cleanup acknowledgment, 100ms fallback and original timing limits remain unchanged. VM execution tests use the actual bootstrap script to verify ordering and private acknowledgment; they do not prove descendant CPU termination. **20 unit tests, lint, strict app/service-worker typechecks and build passed**. Current frozen bundle is **`index-Br5Xj04R.js`**.

[Full actual installed-Chrome retest](evidence/automated-2026-09-14T09-08-43.812Z.json): **34 passed / 15 failed**. Passed subsets include private control/forged output, missing-ack restart, supersession, actual old-output replay, rapid source input, narrow reflow/motion/frame proxy, offline preparation, exact 8/6/6 update mix, source-storage recovery, owned-renderer crash recovery, opaque request/storage denial and post-update online authority. Failed cases include retained nested targets after 1s, expected syntax/runtime/valid results timing out, nested capability/witness checks, useful preview/fresh preview/100 preview cycles, 100 Worker cycles and integrated Q01/owner snapshots. Dedicated request/storage probes also time out; those failed cases do not establish successful containment or forbidden access. No status assertion was waived. Different failure counts do not establish improvement or regression causation under uncontrolled machine load.

Root `pnpm test`, `pnpm lint`, `pnpm typecheck` were retried and remain unavailable because pnpm is not on PATH; no root application manifest is introduced. Prototype checks above actually ran.


### Final continuation evidence and progression - 2026-09-15

[Corrected affected bundled checks](evidence/automated-2026-09-14T09-12-43.433Z.json): **31 passed / 4 failed / 1 skipped**. Chromium 11 passed/1 failed; Firefox 11 passed/1 skipped; WebKit 9 passed/3 failed. Actual prior-output replay passes all three. The failures are Chromium nested-target cleanup, WebKit page creation before an intended CDP-only skip, WebKit initialization crash before the loop test, and the unchanged WebKit offline reload error. Initialization failures do not prove successful or failed learner containment; the affected procedures were not completed. Original failures remain retained.

The CDP-only test now declares unsupported Firefox/WebKit before page fixtures/hooks execute. [Focused classification verification](evidence/automated-2026-09-14T16-17-27.833Z.json): **2 skipped**, not two passing target-lifecycle tests. The supported Chromium test retains every original assertion, including zero targets and recovery <1s. Lint and strict typechecks passed after the test-fixture correction. This corrects unsupported instrumentation setup without waiving the Chromium failure or the separate WebKit initialization/offline failures.

[Independent current native diagnostic](evidence/native-chrome-2026-09-14T09-15-55.779Z.json) completed **100/100 valid Worker cycles** (maximum 301.4ms) and **100/100 preview cycles** (maximum readiness 460.3ms), source retained. However only **7/10 hostile-loop witnesses** were observed; trials 0/1/5 remain unwitnessed. Fresh runs after trials 0/1/4/5 **timed out** at 2006.9/2112.7/2016.6/2009.3ms. These failed fresh runs and the 34/15 full-suite result prevent Proceed despite later stable cycles. Renderer counts were 3 before, 4 after cycles, 4 after 10s and 4 after 30s natural idle; no hard quota or new grace period is inferred.

Actual native 200% and 400% zoom/geometry and exact source were observed. At 200% local Check passed; at 400% it timed out, so the combined essential-flow verdict is failed/inconclusive, not a passing accessibility gate. The captured 400% surface was visually inspected: essential Run/Check/Stop controls remain readable with vertical scrolling. This is visual/geometry evidence, not assistive-technology speech or a passing local Check. Minimize/restore never established hidden document state; background behavior remains inconclusive.

The same current native record passed task-only PWA installation/standalone access, all **8 reload + 6 update + 6 multiclient** source/immutable-pending cycles and installed-app origin-refused relaunch/provisional check. Own PWA uninstalled and profile removed. [Latest native ten-cold-process attempt](evidence/cold-origin-2026-09-14T16-19-07.879Z.json) failed at a valid provisional Check, **0/10 completed trials**, profile removed; the single installed-app relaunch does not satisfy that ten-trial substitute. Compiler verification overlapped initial preparation of this last diagnostic; no failure cause is attributed and no original physical B07 pass is claimed. [Renewed inventory](evidence/windows-inventory-2026-09-14T16-18-35.4467946Z.json) confirms the same exact Windows/hardware and Chrome 153.0.8010.37; Firefox/other physical/AT access remains unconfirmed. Raw UTC timestamps are retained; this continuation status uses the local 2026-09-15 date.

**Phase 1 remains 22/34 complete and Redesign/no-go.** P11/B01-B09 are unchanged. S01-S06 remain proposed, not approved/applied; unavailable hardware/AT and new dated owner result review are still incomplete. Next: explicit review of the [persistent dedicated-bootstrap/public-offline experiment contract](proposed-bootstrap-revision.md), then revise D3/charter and implement/retest only if authorized. This is the existing second candidate, not a third mechanism or a production selection. No policy approval, S01-S06 acceptance, F01/F02 selection, independent security audit, Proceed, Phase 2, Sync or Archive is inferred from Continue. PR #6 remains draft.
